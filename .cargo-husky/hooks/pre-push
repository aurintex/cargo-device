#!/usr/bin/env sh
# Final gate before code leaves the machine: fmt + clippy (all targets) + tests.
set -e
cd "$(git rev-parse --show-toplevel)"
cargo fmt --all -- --check
cargo clippy --all-targets -- -D warnings
cargo test
